dist: bionic
matrix:
  include:
    - name: fedora
      language: minimal
      services: docker
      install:
        - travis_retry docker build --rm -t cdt -f Dockerfile-fedora .
      script:
        - docker run -t cdt meson build
        - docker run -t cdt ninja -C build
  allow_failures:
  fast_finish: true
